server:
  port: 0

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3599/bindery_shop?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: admin
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
    properties:
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect

logging:
  level:
    org.hibernate.SQL: warn
