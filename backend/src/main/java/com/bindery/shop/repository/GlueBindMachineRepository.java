package com.bindery.shop.repository;

import com.bindery.shop.entity.GlueBindMachine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GlueBindMachineRepository extends JpaRepository<GlueBindMachine, Long> {
}
